package net.blogjava.mobile;

import android.app.Activity;
import android.os.Bundle;


public class Main extends Activity
{
	/** Called when the activity is first created. */
	@Override
	public void onCreate(Bundle savedInstanceState)
	{
		super.onCreate(savedInstanceState);
		setContentView(R.layout.main);
		
	//TextView textView = (TextView)
		//addPreferencesFromResource(R.xml.preference_dependencies);
//savedInstanceState.putString("xx", "yy");
//	setTitle(savedInstanceState.getString("wifi"));	
	//	CheckBox checkbox =(CheckBox) findViewById(R.id.checkbox);

		//setTitle(String.valueOf(getPreferenceManager().findPreference("wifi").getLayoutResource()));
		
		//setTitle(String.valueOf(savedInstanceState));
		
	}

} 